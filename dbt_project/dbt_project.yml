name: 'olist_transformation'
version: '1.0.0'
config-version: 2

profile: 'olist_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_transformation:
    staging:
      +materialized: view
    marts:
      +materialized: table
