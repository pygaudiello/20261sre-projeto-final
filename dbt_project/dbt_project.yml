name: 'northwind_transformation'
version: '1.0.0'
config-version: 2

profile: 'northwind_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  northwind_transformation:
    staging:
      +materialized: view
    marts:
      +materialized: table
